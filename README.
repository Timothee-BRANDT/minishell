Parsing de notre buffer;

1) Chercher une cote ou une double quote
2) Cercher la simple ou la double correspondante, si aucun match, return error + exit programme.
    2.1) si match avec simple => socker le ' argument blabla ' dans la liste;
    2.2) si match avec une double => idem stockage dans token + expand les $variable;

3) Else (j'ai trouver aucune quote)
    ft_split(buffer, ' ');

